const r = await tools.mcp_chrome_devtools.evaluate_script({ function: "function() { var yaml = 'customer_number: 100\\ncustomer_long_name: Acme Corp\\n'; var container = document.createElement('div'); document.body.appendChild(container); var currentYaml = yaml; var cf = globalThis.__configForm; cf.init(container, function() { return currentYaml; }, function(y) { currentYaml = y; }, {}); var nameInput = container.querySelector('[data-yaml-key=\"customer_long_name\"]'); var before = nameInput.value; currentYaml = 'customer_number: 200\\ncustomer_long_name: Changed In Raw\\n'; var ok = cf.switchToForm(); var after = nameInput.value; var numInput = container.querySelector('[data-yaml-key=\"customer_number\"]'); container.remove(); return JSON.stringify({ before: before, after: after, numAfter: numInput.value, switchOk: ok }); }" });
console.log("Raw→Form:", r.content[0].text);
